apiVersion: v1
kind: Service
metadata:
  name: {{ include "heos-control.name" . }}
spec:
  type: ClusterIP
  selector:
    {{- include "heos-control.selector" . | nindent 4 }}
  ports:
    - name: https
      port: {{ .Values.api.port }}
      targetPort: https
      appProtocol: https
